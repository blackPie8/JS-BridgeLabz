// a

// function inchestoFeet(inches){
//     return inches / 12
// }

// let inches = 42
// let feet = inchestoFeet(inches)
// console.log(`${inches} Inches to Feet is ${feet}`);


// b

function feetToMeters(feet){
    return feet*0.3048
}

let length = 60
let breadth = 40

let lengthInMeter = feetToMeters(60)
let breadthInMeter = feetToMeters(40)

// console.log(`${length} x ${breadth} feet is ${lengthInMeter} x ${breadthInMeter} in meters`);


// c

let area = (lengthInMeter*breadthInMeter) * 25

function metersToAcres(area){
    return meters / 4047
}

console.log(`Area of 25 such plots would be ${area.toFixed(2)} Acres`)
